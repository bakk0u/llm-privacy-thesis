# thesis_privacy_rebuild

Reconstructed thesis project for prompt-based privacy leakage mitigation in vehicular telematics using local LLMs via Ollama.

## 1. Install dependencies
```bash
pip install -r requirements.txt